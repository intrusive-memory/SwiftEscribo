- an item with a continuation
  line that wraps

- an item followed by a fence

  ```text
  fenced inside an item
  ```

> a quote holding a list
> - quoted item one
> - quoted item two

1. an ordered item

   > a quote inside an ordered item

Last paragraph.
